const sendOtp ; // will generate OTP and send it to the user

const validateOtp;  // will validate the OTP sent to the user

const createUser; //will create a new user in the database

const sendGuestOtp;  // will create a guest user in the database

const validateGuestOtp;  // will validate the OTP sent to the guest user

const login;  // will login the user

